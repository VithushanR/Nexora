"""Test package for the backend.

TODO: add unit tests per agent (agents/), per source client (sources/),
and integration tests for the assembled graph (graph/build_graph.py) and
routers (routers/).
"""
