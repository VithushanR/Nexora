// Domain input screen: user enters a research domain, submits to POST /search,
// then navigates to SelectionPage with the returned run_id + candidates.
// TODO: build the form, call api/client.ts, handle loading/error states.

export default function SearchPage() {
  // TODO: implement
  return null;
}
